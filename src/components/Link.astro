---
const { href, class: className, showActive = false, ...props } = Astro.props;
const currentPath = Astro.url.pathname;
---

<a
  href={href}
  class:list={[
    "transition-colors hover:text-mocha-green",
    { "text-mocha-green": currentPath.includes(href) && showActive },
    className,
  ]}
  {...props}
>
  <slot />
</a>
